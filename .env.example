# The public origin used in canonical and Open Graph URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Grove appends sponsor intents, bot submissions, likes, and add clicks here.
# Mount this path on persistent storage in production.
GROVE_DATA_DIR=.data
